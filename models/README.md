# This directory holds the trained emotion model (ONNX).
# The model file is NOT tracked by git — it's generated by the serena_model project.
#
# To deploy a trained model:
#   cp ../serena_model/models/emotion_model.onnx ./emotion_model.onnx
