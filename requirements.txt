# ==============================================================================
# Serena Backend — Python Dependencies
# ==============================================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9      # Required for form-data / file uploads

# --- Database (ORM + Driver) ---
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0               # Async PostgreSQL driver (used by the app)
psycopg2-binary==2.9.9        # Sync PostgreSQL driver (used by Alembic migrations)
alembic==1.13.1               # Database schema migration tool

# --- Data Validation & Settings ---
pydantic==2.7.1
pydantic-settings==2.2.1      # .env file support for Pydantic

# --- Authentication & Security ---
python-jose[cryptography]==3.3.0  # JWT encoding/decoding
bcrypt==3.2.2                     # Pin to 3.2.2 for passlib compatibility
passlib[bcrypt]==1.7.4            # Password hashing (bcrypt)

# --- Async Task Queue ---
celery[redis]==5.3.6          # Distributed task queue
redis==5.0.4                  # Redis client for Celery broker & result backend

# --- Cloud Storage ---
cloudinary==1.39.1            # Cloudinary SDK for media management

# --- PDF Generation ---
weasyprint==62.1              # HTML/CSS to PDF renderer

# --- Logging ---
loguru==0.7.2                 # Structured, beautiful logging

# --- HTTP Client ---
httpx==0.27.0                 # Async HTTP client (used in tests)

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# --- Data Generation (Seeders) ---
faker==24.11.0                # Realistic fake data for seeders

# --- Emotion Detection (Inference Only) ---
onnxruntime==1.18.0           # ONNX model inference (CPU, lightweight)
mediapipe==0.10.14            # Face detection (MediaPipe BlazeFace)
opencv-python-headless==4.10.0.84  # Image/video processing (no GUI)
numpy>=1.26.0                 # Array operations
